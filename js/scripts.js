function add(number1, number2) {
  return number1 + number2;
}

const number1 = parseInt(prompt("enter a number pleez"));
const number2 = parseInt(prompt("enter another numberz"));

window.alert(add(number1,number2,));

